# Audio Timestamp Verifier - Python Dependencies

# Required
requests>=2.28.0

# Recommended (faster Levenshtein distance calculation)
# Falls back to pure Python implementation if not available
python-Levenshtein>=0.20.0

# Optional: For advanced text processing
# Uncomment if needed for your use case
# difflib (built-in)
# fuzzywuzzy>=0.18.0
